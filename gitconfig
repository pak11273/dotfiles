[alias]
		am = git add -A && git commit -m 
    s = status
    co = checkout
    cm = commit
    s = status
    br = branch
    hist = log --pretty=format:'%h %ad | %s%d [%an]' --graph --date=short
    type = cat-file -t
    dump = cat-file -p
[user]
    email = pak11273@gmail.com
    name = pak11273
[color]
    ui = auto
[push]
    default = simple

[credential]
	helper = cache
